# 📧 Spam Message Classifier (Machine Learning Project)

A complete end-to-end **Machine Learning text-classification project** that detects whether a message is **Spam** or **Not Spam (Ham)**.  
This project uses **TF-IDF Vectorization** + **Logistic Regression** to build a lightweight, fast, and highly accurate spam detector.

---

## 🚀 Features

- Cleans and preprocesses text messages  
- Converts text into numerical vectors using **TF-IDF**  
- Trains a **Logistic Regression classifier**  
- Evaluates accuracy using test data  
- Saves the trained model and vectorizer (`.pkl` files)  
- Can classify any new message instantly  
- Ready for deployment (API, Web App, Mobile App)

---
